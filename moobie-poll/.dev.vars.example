# Copy to .dev.vars for local `wrangler dev`. The real value is set in production
# with `wrangler secret put TRIGGER_KEY` and never committed.
# Guards the on-demand GET /poll?key=... trigger endpoint.
TRIGGER_KEY=pick-any-local-secret

# dependencies
node_modules/

# build output
dist/
.astro/

# wrangler local state + generated types
.wrangler/
worker-configuration.d.ts

# secrets — never commit (but keep the committed .dev.vars.example template)
.dev.vars
.dev.vars.*
!.dev.vars.example
*.env
.env
.env.*

# local scratch
.scratch-*

# os / editor
.DS_Store
*.log

{
  "name": "moobie",
  "private": true,
  "version": "1.0.0",
  "description": "Discord bot + web dashboard that logs a friend group's Letterboxd diaries.",
  "scripts": {
    "db:remote": "wrangler d1 execute moobie --remote --file=./db/schema.sql"
  }
}

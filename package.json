{
  "name": "moobie",
  "private": true,
  "version": "1.0.0",
  "description": "letterboxd rss feed scraper discord bot + ui",
  "scripts": {
    "dev": "pnpm --filter moobie-app dev",
    "db:remote": "wrangler d1 execute moobie --remote --file=./db/schema.sql"
  }
}
